# AegisBox Operational Scripts

This directory contains shell and automation scripts used for rootfs creation, provisioning, and deployment.

- `setup-rootfs.sh` (Milestone 2): Automated builder for minimal isolated Python root filesystems.
- `deploy.sh` (Milestone 10): Linux server deployment automation and systemd service updates.
